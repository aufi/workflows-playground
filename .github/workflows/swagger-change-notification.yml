on:
  pull_request

jobs:
  example_comment_pr:
    runs-on: ubuntu-latest
    name: Notify Marek on file change
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Comment PR
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            :mega: There is a TEST files change, please consider re-generating your API client. @aufi
